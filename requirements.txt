# TerraTwin — dependency manifest
#
# Install (Windows/macOS/Linux CPU or NVIDIA CUDA):
#   conda create -n digital-twin python=3.10
#   conda activate digital-twin
#   pip install -r requirements.txt
#
# For CUDA (GTX 1660 Super): install PyTorch first from the CUDA index,
# then the rest of the file:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt          # will see torch already satisfied
#
# AMD MI300X (ROCm) fallback:
#   pip install --index-url https://download.pytorch.org/whl/rocm6.2 torch torchvision
#   pip install -r requirements.txt
#
# Frozen 2026-09-09 from envs/digital-twin (Python 3.10) — this is the exact
# environment that produced the Stage-1 model (val IoU 0.8040 / test IoU 0.8062).
# torch/torchvision are pinned to the cu121 builds actually installed; the
# previous 2.13.0/0.28.0 pins were never available on that index.
# Only project-relevant deps are pinned; unrelated conda packages omitted.

# core
numpy==2.2.6
Pillow==12.2.0
matplotlib==3.10.9
tqdm==4.70.0

# PyTorch — installed from the cu121 index (see notes above). The local build
# carries the +cu121 suffix; a CPU or ROCm install of the same version works
# identically for this pipeline.
torch==2.5.1
torchvision==0.20.1
sympy==1.13.1

# segmentation
segmentation-models-pytorch==0.5.0
timm==1.0.29

# geospatial
rasterio==1.4.4
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.1
affine==2.4.0

# also pulled in by geopandas / rasterio but explicit for clarity
# scikit-learn is a transitive dep of some viz helpers; keep if needed
# scikit-learn==1.7.2
