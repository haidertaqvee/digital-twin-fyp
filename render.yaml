services:
  - type: web
    name: terratwin-sos
    env: python
    buildCommand: pip install -r requirements-server.txt
    startCommand: python src/server.py --port $PORT --host 0.0.0.0
    plan: free
    autoDeploy: true
