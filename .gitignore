data/raw/
data/processed/
models/
__pycache__/
*.pyc
.ipynb_checkpoints/
.env
*.pth
*.tar.gz

# Checked-in conda environment (machine-specific, not source)
envs/

# Claude Code — project-local settings are machine-specific, do not commit
.claude/settings.local.json

# Vendored upstream repos — separate git clones, not submodules
satnerf/
sat-bundleadjust/
